import React from 'react';

const Exit = () => {
  return (
    <div>
      <button onClick={() => {/* obsługa wylogowania */}}>
        Wyloguj się
      </button>
    </div>
  );
};

export default Exit;
